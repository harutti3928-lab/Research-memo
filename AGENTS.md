# AGENTS.md

このファイルは、AIエージェントが作業するときの基本ワークフロールールを定義する。
出力は日本語ですること。

## 1. 進め方
- 要件を最初に要約し、何を実行するかを短く宣言してから作業を開始する。
- 可能な限り、調査・編集・検証までを1ターンで完了する。
- 3〜5回程度のツール操作ごと、またはまとまった編集後に進捗を簡潔に報告する。
- 並列で実行できる読み取り系操作は並列化する。
- 足りない情報があったら質問する。

## 2. 実装ルール
- 既存仕様を優先し、不要な設計変更をしない。
- 変更は最小限にし、関連しないファイルは触らない。
- すでに実装済みの関数やプログラムを変更する場合は、改変によるリスクを先に告知し、確認を取ってから進める。
- 単一ファイルの小変更はパッチ編集を優先する。
- 文字コードは原則ASCIIを使い、必要時のみUnicodeを使う。

## 3. Git運用
- 破壊的コマンド（例: `git reset --hard`）を無断で実行しない。
- 自分が作っていない差分を勝手に巻き戻さない。
- 予期しない変更を見つけたら作業を止め、方針を確認する。
- github使用時は、差分を確認して上書きされないようにする。

## 4. 品質確認
- 変更後は可能な範囲で実行確認・エラーチェックを行う。
- 実行できなかった確認事項は、未実施として明示する。
- 回答では、何を変更したかと理由を簡潔に示す。
- 式を出力する際は、読みやすい数式形式（例: Markdownのインライン数式 `$...$` やブロック数式 `$$...$$`）を使う。

## 5. セーフティ
- 有害・違法・差別的な内容には対応しない。
- 秘密情報・認証情報を出力しない。
- 著作権侵害に当たる内容は生成しない。

## 6. コマンド
- rg はないのでgrepなどを使う