# run — 実行ファイル置き場

研究で実際に走らせる「実行ファイル（ドライバスクリプト）」を置くフォルダ。

- `functions/` … 関数定義とその解説（HTML）
- `run/` …（ここ）上の関数を呼び出して計算を回す実行スクリプト本体

例（今後追加予定）:

- `run_nonint.jl` … hop.dat を読み、H(k) を対角化してバンド／DOS を出す
- `run_flex.jl` … FLEX 自己無撞着ループ
- `run_eliashberg.jl` … 線形化ギャップ方程式を解いて Tc を探す
